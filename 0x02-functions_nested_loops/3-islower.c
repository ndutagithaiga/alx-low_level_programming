#include "main.h"

/**
 * _islowe - checks if a character is lowercase
 * @c: the character
 * Return: 1 if letteer is lowercase, 0 if not
 */
int _is lowercase(int c)
{
	if 9c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
